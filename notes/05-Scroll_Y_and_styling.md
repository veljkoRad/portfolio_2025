### isActive ItemList
1. Creating state which catches current windowsY height in pixels ('scrollY')
    - useState to store variable
    - useEffect  after mounting create function that setScrollY, than add event on everyscrole to caches new scrollY
2. Add offset to every ListItem 
3. Create condition for color styiling which checks  specific heights